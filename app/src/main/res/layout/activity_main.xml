<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:fitsSystemWindows="true"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/gradient_background"
    tools:context=".MainActivity"
    tools:ignore="ExtraText">

    <!-- Top Bar moderna con título, logo e ícono -->

    <!-- Tarjeta que contiene la vista previa de la cámara con sombra y bordes redondeados -->

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/topAppBar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@drawable/gradient_toolbar"
        app:layout_constraintTop_toTopOf="parent"
        app:menu="@menu/top_bar_menu"
        app:title="Escanear PIN"
        app:titleCentered="true"
        app:titleTextAppearance="@style/TextAppearance.Material3.HeadlineMedium.Bold"
        app:titleTextColor="#FAFAFA"
        tools:ignore="MissingConstraints"
        tools:layout_editor_absoluteX="0dp" />

    <androidx.cardview.widget.CardView
        android:id="@+id/cameraCard"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/topAppBar"
        app:layout_constraintBottom_toTopOf="@+id/textCard"
        app:layout_constraintHeight_percent="0.35"
        app:cardBackgroundColor="@android:color/white"
        android:foreground="?android:attr/selectableItemBackground"
        tools:ignore="NotSibling">

        <FrameLayout
            android:id="@+id/previewContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.camera.view.PreviewView
                android:id="@+id/cameraPreview"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <View
                android:id="@+id/roiOverlay"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/roi_mask" />

            <com.google.android.material.card.MaterialCardView
                android:layout_width="340dp"
                android:layout_height="86dp"
                android:layout_gravity="center"
                android:alpha="0.9"
                app:cardBackgroundColor="@android:color/transparent"
                app:cardCornerRadius="24dp"
                app:cardElevation="0dp"
                app:strokeColor="#08F412"
                app:strokeWidth="3dp" />

            <!-- Overlay con máscara oscura (nuevo) -->

            <!-- Esquinas animadas del ROI -->
        </FrameLayout>
    </androidx.cardview.widget.CardView>

    <!-- Tarjeta para mostrar el número detectado con estilo elevado -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/textCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="16dp"
        app:cardElevation="4dp"
        android:layout_margin="16dp"
        app:strokeWidth="1dp"
        app:strokeColor="#031111"
        app:cardBackgroundColor="#FBC02D"
        app:layout_constraintTop_toBottomOf="@id/cameraCard"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="103dp"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:text="@string/n_mero_detectado"
                android:textColor="#414E4E"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/txtConsola"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:ellipsize="end"
                android:fontFamily="monospace"
                android:maxLines="1"
                android:text="@string/a"
                android:textColor="#0D1010"
                android:textSize="28sp"
                android:textStyle="bold"
                android:letterSpacing="0.05" />
        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Botones de acción con estilo Material 3 (más modernos) -->
    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/buttonGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginBottom="60dp"
        app:singleSelection="false"
        app:layout_constraintBottom_toTopOf="@+id/tvInstruction"
        app:layout_constraintTop_toBottomOf="@id/textCard"
        app:layout_constraintVertical_bias="1"
        android:gravity="center">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCopy"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:text="@string/copiar"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            app:icon="@drawable/ic_content_copy"
            app:iconGravity="textTop"
            app:iconTint="@android:color/white"
            app:iconPadding="8dp"
            android:backgroundTint="#5DB0F5"
            app:cornerRadius="12dp"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnReset"
            style="@style/Widget.Material3.Button"
            android:layout_width="0dp"
            android:layout_height="80dp"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:text="@string/reintentar"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            app:icon="@drawable/ic_refresh"
            app:iconGravity="textTop"
            app:iconTint="@android:color/white"
            app:iconPadding="8dp"
            android:backgroundTint="#FF9800"
            app:cornerRadius="12dp"/>

    </com.google.android.material.button.MaterialButtonToggleGroup>

    <!-- Texto de instrucción inferior -->
    <TextView
        android:id="@+id/tvInstruction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="24dp"
        android:text="@string/centre_el_pin_dentro_del_cuadro_para_continuar"
        android:textColor="#FAFAFA"
        android:textSize="14sp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:padding="8dp"
        android:background="@drawable/instruction_background"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>